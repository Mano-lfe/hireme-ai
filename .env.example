# Optional — the app remains fully functional in demonstration mode without these variables.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5

# Enable real accounts and cloud history with Supabase.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
